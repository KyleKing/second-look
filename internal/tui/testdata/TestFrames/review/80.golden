kyleking/jj-diff #42                          0% · 1 ready · 1 draft · 1 skipped
                                                                                
internal/vcs/diff.go
  @@ -14,6 +14,7 @@ func Parse(r io.Reader) ([]Hunk, error) {
 14       first := 1
     │ ◐ question
     │ Is first still 1 after the change?
     │ · Unverified: I have not read the caller.
 15 -     lines := split(r)
 15 +     lines, err := split(r)
     │ ● major
     │ split can fail now, so this has to check err.
 16 +     if err != nil {
 17       last := 4

internal/vcs/git.go
  @@ -200,3 +201,3 @@ func Head() string {
201       return head
     │ ○ nit — Out of scope for this change.
     │ Rename this to headSHA.



 n/p hunk }/{ file tab comment e edit r/d/x state S submit ? help